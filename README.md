# Ticket-bot

بوت تذاكر احترافي كامل يعتمد على **Slash Commands** عربية، مع دعم الأزرار، القوائم، والمودالز لإدارة كافة أنواع التذاكر المذكورة في المواصفات.

## التثبيت والتشغيل

1. تثبيت الاعتمادات:
   ```bash
   npm install
   ```
2. إنشاء ملف `.env` بالقيم التالية:
   ```
   TOKEN=توكن_البوت
   CLIENT_ID=معرف_التطبيق
   GUILD_ID=معرف_السيرفر_للتسجيل_المحلي
   ```
3. نشر أوامر السلاش داخل السيرفر:
   ```bash
   npm run register
   ```
4. تشغيل البوت:
   ```bash
   npm start
   ```

> يتم حفظ حالة النظام في `data/state.json` تلقائياً (مستبعدة من التحكم بالإصدارات).

## أهم الأوامر العربية (Slash)

- `/إنشاء-داشبورد` – إنشاء لوحة تذاكر جديدة.
- `/إضافة-زر`، `/حذف-زر`، `/عرض-الداشبوردات` – إدارة اللوحات والأزرار.
- `/ربط-رتبة-بنوع`، `/عرض-الأدوار-لنوع`، `/حذف-رتبة-من-نوع` – ربط أنواع التذاكر بالرتب.
- `/إلغاء-الاستلام`، `/نقل-التذكرة` – التحكم بالاستلام والنقل.
- `/تغيير-حالة-التذكرة`، `/إضافة-وسم`، `/حذف-وسم` – تحديث حالة التذكرة ووسومها.
- `/إعداد-أولي`، `/إعدادات-الاستلام`، `/إعدادات-الإغلاق-التلقائي`، `/إعدادات-التذكير`، `/تبديل-وضع-النظام` – إعدادات وتشغيل النظام.
- `/بحث-تذكرة`، `/تذاكري`، `/ملف-دعم`، `/تقارير-التذاكر` – البحث والتقارير والملفات الشخصية.
- `/فتح-تذكرة`، `/إغلاق-تذكرتي`، `/مركز-المساعدة` – أوامر الأعضاء.
- `/إعدادات-الحدود`، `/حظر-من-التذاكر`، `/إلغاء-حظر-التذاكر`، `/قائمة-المحظورين-من-التذاكر` – مكافحة السبام وإدارة الحظر.
- `/ملاحظة-داخلية` – إضافة ملاحظات لا يراها الأعضاء.

## ملاحظات

- جميع التفاعلات تعمل داخل السيرفر (لا يدعم الـ DM).
- زر `📥 استلام التذكرة` وإغلاق التذكرة متاحان داخل قناة التذكرة مع سجل زمني وتذكيرات وإغلاق تلقائي حسب الإعدادات.
